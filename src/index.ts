const index = async () => {
  console.log("Use the following commands to run the examples:")
  console.log("  npm run create: Create the agents and their DIDs")
  console.log("  npm run issue: Issue a credential to the holder agent")
  console.log("  npm run verify: Present and verify the credential")
}

index();
